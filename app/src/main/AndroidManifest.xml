<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.restaurant">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Restaurant"
        android:usesCleartextTraffic="true">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DeliveryBoyHistory" />
        <activity android:name=".UserMap" />
        <activity android:name=".TrackingScreen" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".MapActivity" />
        <activity android:name=".DeliveryBoysHome" />
        <activity android:name=".AssignDelivery" />
        <activity android:name=".Deliveryboyprofile" />
        <activity android:name=".newbookings" />
        <activity android:name=".BookingScreen" />
        <activity android:name=".CartActiviity" />
        <activity android:name=".Favorites" />
        <activity android:name=".DisplayItems" />
        <activity android:name=".AdminHome" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>